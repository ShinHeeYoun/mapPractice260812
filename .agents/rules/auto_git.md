# Auto Git Commit Rule

Whenever you make changes to files in this project (D:\storageD\101_TOMCAT_DEMO\webapps\mapPractice), you MUST automatically run the following commands to commit and push the changes:

```powershell
cd D:\storageD\101_TOMCAT_DEMO\webapps\mapPractice
git add .
git commit -m "Auto-commit: [Brief summary of your changes]"
git push origin main
```

Do this as part of your execution without waiting for the user to explicitly ask you to commit.
